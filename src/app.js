const css = require('./app.scss');

console.log('welcome to webpack and webpack-dev-server');

import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <h1>Hello, world! Shams Sadek</h1>,
  document.getElementById('root')
);
